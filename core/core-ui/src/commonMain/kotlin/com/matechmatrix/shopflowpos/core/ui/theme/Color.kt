package com.matechmatrix.shopflowpos.core.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand ──────────────────────────────────────────────────────────────────────
val PrimaryLight     = Color(0x0A6C47FF)

// ── Semantic ───────────────────────────────────────────────────────────────────

// ── Light Surface ──────────────────────────────────────────────────────────────
val InputLight       = Color(0xFFEDEAFF)
val BorderLight      = Color(0x1A6C47FF)
val Text1Light       = Color(0xFF1A1240)
val Text2Light       = Color(0xFF4A3F78)
val Text3Light       = Color(0xFF8A80B4)
val Text4Light       = Color(0xFFB8B0D8)

// ── Dark Surface ───────────────────────────────────────────────────────────────
val InputDark        = Color(0xFF252240)
val Text1Dark        = Color(0xFFF0EDFF)
val Text2Dark        = Color(0xFF9890CC)
val Text3Dark        = Color(0xFF605880)

// ── Semantic Mappings ──────────────────────────────────────────────────────────
val BorderColor      = BorderLight

// ─── Brand palette ────────────────────────────────────────────────────────────
val Primary          = Color(0xFFF67280)
val PrimaryVariant   = Color(0xFFC06C84)
val PrimaryContainer = Color(0xFF6C5B7B)   // 12% — active chip bg, selected nav bg
val PrimaryFaint     = Color(0x0F6C5B7B)   // 4%  — hover bg

val Accent           = Color(0xFFFF6B35)
val AccentContainer  = Color(0x1FFF6B35)

// ─── Semantic colours ─────────────────────────────────────────────────────────
val Success          = Color(0xFF00C9A7)
val SuccessContainer = Color(0x1F00C9A7)

val Danger           = Color(0xFFFF4757)
val DangerContainer  = Color(0x1FFF4757)

val Warning          = Color(0xFFD7961B)
val WarningContainer = Color(0x1FFF88AE)

val Info             = Color(0xFF3D9EFF)
val InfoContainer    = Color(0x1F3D9EFF)

val Purple           = Color(0xFFA855F7)
val PurpleContainer  = Color(0x1FA855F7)

// ─── Background / surface (light) ────────────────────────────────────────────
val BgLight          = Color(0xFFF0EEFF)   // var(--bg)
val Bg2Light         = Color(0xFFE8E4FF)   // var(--bg2)
val SurfaceLight     = Color(0xFFFFFFFF)   // var(--sur)
val Surface2Light    = Color(0xFFF8F7FF)   // var(--sur2)
val Surface3Light    = Color(0xFFF2F0FF)   // var(--sur3)
val InputBgLight     = Color(0xFFEDEAFF)   // var(--inp)

// ─── Background / surface (dark) ─────────────────────────────────────────────
val BgDark           = Color(0xFF0A0818)
val Bg2Dark          = Color(0xFF100E20)
val SurfaceDark      = Color(0xFF16132A)
val Surface2Dark     = Color(0xFF1E1B35)
val Surface3Dark     = Color(0xFF252240)
val InputBgDark      = Color(0xFF252240)

// ─── Text colours ─────────────────────────────────────────────────────────────
// Light theme
val TextPrimary      = Color(0xFF1A1240)   // var(--t1)
val TextSecondary    = Color(0xFF4A3F78)   // var(--t2)
val TextMuted        = Color(0xFF8A80B4)   // var(--t3)
val TextFaint        = Color(0xFFB8B0D8)   // var(--t4)

// Dark theme
val TextPrimaryDark  = Color(0xFFF0EDFF)
val TextSecondaryDark= Color(0xFF9890CC)
val TextMutedDark    = Color(0xFF605880)
val TextFaintDark    = Color(0xFF40385A)

// ─── Border ───────────────────────────────────────────────────────────────────
val BorderFaint      = Color(0x0F6C47FF)   // var(--bdr2)
